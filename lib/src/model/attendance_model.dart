class AttendanceModel {
  double   ? totalAttendance;
  String? latt;
  String? patt;
  String? subject;

  AttendanceModel({
    this.totalAttendance,
    this.latt,
    this.patt,
    this.subject,
  });
}
